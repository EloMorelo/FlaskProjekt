- hosts: app
  become: yes
  roles:
    - app
    - nginx
    - install-filebeat
    - role: hostname
      vars:
        hostname: app.lab.local
    - role: hardening
    - set-iptables
    - role: install-rsyslog
