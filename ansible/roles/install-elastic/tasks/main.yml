---
- name: install elasticsearch
  apt:
    name:
      - elasticsearch
      - kibana
    state: latest

- name: enable and start elasticsearch service
  systemd:
    name: elasticsearch
    enabled: true
    state: started
