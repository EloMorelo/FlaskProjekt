---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install XFCE and remote access packages
  apt:
    name:
      - dbus-x11
      - spice-vdagent
      - xfce4
      - xfce4-goodies
      - xinit
      - xorg
      - xrdp
      - x11-xserver-utils
    state: present
    install_recommends: no

- name: Create xrdp start script to launch XFCE
  copy:
    dest: /etc/xrdp/startwm.sh
    content: |
      #!/bin/sh
      unset DBUS_SESSION_BUS_ADDRESS
      unset XDG_RUNTIME_DIR
      if [ -r /etc/profile ]; then
        . /etc/profile
      fi
      # exec startxfce4
    owner: root
    group: root
    mode: '0755'

- name: Ensure xrdp service is enabled and started
  systemd:
    name: xrdp
    enabled: yes
    state: restarted

- name: Ensure spice-vdagent service is enabled and started
  systemd:
    name: spice-vdagent
    enabled: yes
    state: started

- name: Allow RDP through UFW (if ufw present)
  ufw:
    rule: allow
    name: 'OpenSSH'   # keep SSH allowed if present
  when: false

